# CommutePool — Netlify deployment config (Angular PWA)
# base = repo root so pnpm can read pnpm-lock.yaml correctly

[build]
  base    = "/"
  command = "pnpm install --filter pwa... && cd pwa && npx ng build --configuration production"
  publish = "pwa/dist/pwa/browser"

[build.environment]
  NODE_VERSION  = "20"
  PNPM_VERSION  = "9"

# SPA fallback — all routes served by Angular router
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200

# API proxy → Render backend (avoids CORS entirely)
[[redirects]]
  from   = "/api/*"
  to     = "https://commutepool-api.onrender.com/api/:splat"
  status = 200
  force  = true

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options        = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy        = "strict-origin-when-cross-origin"
    Cache-Control          = "public, max-age=0, must-revalidate"

[[headers]]
  for = "/assets/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

[[headers]]
  for = "/ngsw.json"
  [headers.values]
    Cache-Control = "no-cache"
