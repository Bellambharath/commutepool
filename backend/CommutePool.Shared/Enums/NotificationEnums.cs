namespace CommutePool.Shared.Enums;

public enum NotificationCategory { General, TripUpdate, MatchFound, Payment, Safety, Support }
public enum TripDirection { ToOffice, ToHome }
public enum TicketCategory { Trip, Payment, Safety, Account, Other }
